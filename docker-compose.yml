version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Сохранение загруженных файлов между перезапусками
      - uploads_data:/app/uploads
    restart: unless-stopped

volumes:
  uploads_data:
